{% extends 'base.html.twig' %}

{% block title %}Preisangebot
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1>Preisangebot</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Liefertermin</th>
					<td>{{ quotation.deadline ? quotation.deadline|date('Y-m-d') : '' }}</td>
				</tr>
				<tr>
					<th>gesendet</th>
					<td>{{ quotation.isSend ? 'Ja' : 'Nein' }}</td>
				</tr>
			</tbody>
		</table>

		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_quotation_index') }}">Zurück</a>


		<a class="btn btn-primary btn-lg mt-4" href="{{ path('app_quotation_edit', {'id': quotation.id}) }}">Ändern</a>


		{{ include('pages/quotation/_delete_form.html.twig') }}</div>

{% endblock %}
