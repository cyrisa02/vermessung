<form method="post" action="{{ path('app_provider_delete', {'id': provider.id}) }}" onsubmit="return confirm('Wollen Sie den Lieferant löschen?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ provider.id) }}">
	<button class="btn btn-danger btn-lg mt-4">Löschen</button>

</form>
