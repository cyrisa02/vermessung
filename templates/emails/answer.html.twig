<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
	</head>
	<body>

		<h1>
			Saison
			{{ "now"|date("Y") }}
			/
			{{ "now"|date("Y") +1  }}


		</h1>
		<h2>
			Association Sport Détente et Culture</h2>
		<h3>
			{{ quotation.id }}
			nous a écrit un message.
		</h3>
		<h3>
			Son adresse mail:
			{{ quotation.id }}</h3>

		<p>
			Son message :
			{{ quotation.id }}</p>


		Rédigé le :
		{{ "now"|date("d/m/Y") }}</body>
</html></p></body></html>
